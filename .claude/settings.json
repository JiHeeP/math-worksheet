{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | python3 -c 'import json,sys;d=json.load(sys.stdin);print(d.get(\"tool_input\",{}).get(\"file_path\",\"\"))'); case \"$FILE\" in *catalogs/*.js|*generators/*.js|*templates.js|*helpers.js) cd \"$CLAUDE_PROJECT_DIR\" && node .claude/scripts/validate.mjs 500 2>&1 | tail -3 ;; esac"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(node .claude/scripts/validate.mjs:*)",
      "Bash(node .claude/scripts/audit.mjs:*)",
      "Bash(git push origin main:*)"
    ]
  }
}
