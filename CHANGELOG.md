# 변경 이력

## 2026-05-09: 가로셈 왼쪽 정렬

### 변경 내용

- 일반 가로셈(`horiz-box`)과 분수 가로식(`frac-row`)을 가운데 정렬에서 왼쪽 정렬로 변경했다.
- 1·2학년 받아올림/받아내림 가로 연습형(`origin-horiz-practice`)도 왼쪽 정렬로 맞췄다.
- 기존 탭/브라우저 캐시가 예전 CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신했다.

## 2026-05-09: 분수 덧셈·뺄셈 시트 구성과 3단계 숫자 상한 조정

### 변경 내용

- 5-1 5단원 분모 다른 분수 덧셈·뺄셈의 단계별 공통분모 상한을 추가했다. 3단계는 분모 범위는 유지하되 공통분모가 240을 넘지 않도록 제한한다.
- 대분수 덧셈·뺄셈의 가분수 변환 과정에서 변환 후 분자가 과도하게 커지지 않도록 단계별 상한을 둔다.
- 5-1 5단원 `자연수·분수 따로` 풀이 4종은 여러 줄 풀이 전용 렌더링과 2×2 레이아웃을 사용해 한 장에 4문항이 안정적으로 들어가도록 조정했다.
- 단계 풀이형 분수 덧셈·뺄셈 템플릿의 기본 문항 수 메타를 실제 자동 맞춤 문항 수와 맞췄다.
- 기존 탭/브라우저 캐시가 예전 JS/CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신했다.

### 검증

- 5-1 5단원 generator 105,000회 샘플링: 통분 조건(`lcm(d1,d2) < d1*d2`) 위반 0건, 3단계 공통분모 최대 240, 대분수 변환 후 분자 최대 1800.
- `node .claude/scripts/validate.mjs 5000`: 전체 239개 학습지 위반 0건.
- `node .claude/scripts/validate-stages.mjs 5000`: 단계형 62개 학습지 총 865,000회 실행, 위반 0건.
- Playwright 브라우저 검수: 4-2 1단원 + 5-1 5단원 총 29개 옵션, 단계 포함 62케이스에서 overflow 0건, 1문항 이하로 줄어드는 시트 0건.
- 5-1 5단원 `자연수·분수 따로` 4종은 모든 난이도에서 4문항, 2×2 배치로 렌더링 확인.

## 2026-05-08: 통분 분모쌍과 분수 덧셈·뺄셈 풀이 과정 보정

### 변경 내용

- 통분이 필요한 5-1 4·5단원 학습지는 두 분모가 서로소가 아니도록 생성한다. 이제 `lcm(d1, d2) < d1 × d2` 조건을 만족하는 분모쌍만 사용한다.
- 분모 다른 진분수 덧셈·뺄셈의 통분 후 공통분모를 미리 숫자로 보여주지 않고 빈칸으로 둔다.
- 대분수 덧셈·뺄셈 풀이에 `분자 × □ / 분모 × □` 과정을 별도 단계로 추가해, 학생이 통분 과정을 직접 채우는 흐름을 강화했다.

### 검증

- `node --check` 로 `app/js/utils.js`, `app/js/generators/g5-1/u4.js`, `app/js/generators/g5-1/u5.js`, `app/js/templates.js` 문법 검사 통과.
- 통분 분모쌍 sanity check: 5-1 5단원 관련 generator 105,000회 검사, `d1 !== d2`, `gcd(d1,d2) > 1`, `lcm(d1,d2) < d1*d2` 위반 0건.
- `node .claude/scripts/validate.mjs 2000`: 위반 0건.
- `node .claude/scripts/validate-stages.mjs 5000`: 865,000회 실행, 위반 0건.

## 2026-05-08: 숫자 난이도 단계 옵션 도입 (자연수 + 분수)

### 변경 내용

- `worksheet.html` 의 컨트롤바에 **자연수 난이도** / **분수 난이도** 라디오(1·2·3단계) 추가. 학습지가 허용한 단계만 활성, 학습지 전환 시 자동 토글.
- `app/js/utils.js` 에 단계 → 범위 헬퍼 함수 4개 추가:
  - `numberRangeForStage(stage)` — 자연수 피연산자 [lo, hi] (1단계 [1,20], 2단계 [20,50], 3단계 [50,99])
  - `properFracLimitsForStage(stage)` — 진분수 분모 (1단계 [2,10], 2단계 [4,12], 3단계 [6,14])
  - `lcdFracLimitsForStage(stage)` — 통분/약분 분모 (1단계 [2,8], 2단계 [6,20], 3단계 [10,50])
  - `mixedWholeForStage(stage)` — 대분수 자연수부 (1단계 [1,3], 2단계 [2,5], 3단계 [3,6])
- 카탈로그에 `controls.numberDifficulty` 또는 `controls.fractionDifficulty` 메타로 단계 활성화. `차시(...)` 의 defaults 에 한꺼번에 명시 가능.
- 자연수 단계 적용 학습지 (5-1 1단원·1-2 4단원, 9개 학습지):
  - 5-1 1단원: 덧·뺄셈 혼합(순서/괄호/순서·괄호), 곱·나눗셈 혼합(순서), 사칙 혼합(순서/괄호/순서·괄호) — 1·2·3단계
  - 1-2 4단원: 두자리±두자리(받아올림/내림 없음) — 1·2단계 (1학년이라 3단계 부담)
- 분수 단계 적용 학습지 (4-2·5-1·5-2·6-1·6-2 분수 계열, 53개 학습지):
  - 4-2 1단원: 분모 같은 분수 덧·뺄셈, 받아올림/내림 단계 풀이 — 1·2단계
  - 5-1 4단원: 약분(과정·기본), 통분(과정·기본), 분수 크기 비교 — 1·2·3단계
  - 5-1 5단원: 분모 다른 진분수 덧·뺄셈, 대분수 덧·뺄셈(받아올림/내림 변형 포함) — 1·2·3단계
  - 5-2 2단원: 분수 곱셈 7종 (자연수·진분수·대분수·단위분수 조합) + 단계 풀이 7종 — 1·2·3단계
  - 6-1 1단원: 자연수 ÷ 자연수 → 분수, 진분수 ÷ 자연수, 대분수 ÷ 자연수 + 단계 풀이 — 1·2·3단계
  - 6-2 1단원: 진분수 ÷ 진분수, 자연수 ÷ 진분수, 대분수 ÷ 진분수 + 단계 풀이 — 1·2·3단계
- 모든 매개변수화된 generator 가 ctx 인자를 받아 단계별 분포를 따른다. invariant (자연수: 음수/0/소수 결과 금지; 분수: 진분수 분자 < 분모, 통분 분모 일치, 받아내림 후 결과 ≥ 1 등) 는 do-while 재시도(최대 50~200회) 또는 fallback 로 보존.

### 검증

- `node .claude/scripts/validate.mjs 2000`: 478,000회 회귀 테스트 — 위반 0건 (단계 미적용 기본 동작 유지).
- `node .claude/scripts/validate-stages.mjs 5000`: 단계별 회귀 테스트 — **62개 학습지 × 평균 ~3단계 × 5,000회 = 약 865,000회** 실행, **위반 0건** (음수/0/소수 결과 없음, 분모 상한 위반 없음, 무한 루프 없음).
- 단계 전환 캐시 키에 `@nstage-`, `@fstage-` 추가해 단계 변경 시 자동 fit 재계산.
- 컨트롤바 CSS 에 `.is-disabled` 추가해 비활성 단계는 흐리게 표시.

## 2026-05-07: 학습 단계 필터 추가

### 변경 내용

- 연습지 선택 근처에 `전체` / `진단지` / `선수학습` / `본단원` 학습 단계 필터를 추가.
- 현재 단원에 없는 단계는 비활성화하고, 선택 중인 단계가 새 단원에 없으면 `전체`로 자동 복귀하도록 연결.
- 단계 필터가 연습지 목록만 좁히도록 구현해 `새로 만들기`, `이어 붙이기`, `정답 보기`, `인쇄` 흐름은 유지.
- 기존 탭/브라우저 캐시가 예전 JS/CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신.

### 검증

- `node --check app/js/app.js`: 문법 검사 통과.
- `node .claude/scripts/validate.mjs 1000`: 총 위반 사례 0건.
- `node .claude/scripts/audit.mjs`: 깨진 refs 0건, ext 승격 후보 0건, 라벨 중복 0건. 기존 선수학습 미등록 정보 5건은 유지.
- Chrome headless 브라우저에서 5-1 1단원 전체/진단지/선수학습 필터, 5-1 3단원 빈 단계 비활성화와 `전체` 자동 복귀, 2-2 2단원 본단원 필터 유지 확인.
- 모바일 폭에서 학습 단계 버튼 텍스트 넘침과 컨트롤바 가로 overflow 0건 확인.

## 2026-05-07: 곱셈구구 단 선택 컨트롤 추가

### 변경 내용

- `곱셈구구` 계열 학습지를 선택하면 `인쇄` 옆에 2단~9단 선택 버튼이 나타나도록 추가.
- 5-1 2단원 선수학습 `곱셈구구`와 2-2 2단원 곱셈구구 혼합/빈칸 학습지에 선택 단을 연결.
- 선택 단이 자동 문제 수 맞춤 캐시에 반영되도록 생성 context를 렌더러까지 전달.

### 검증

- `node --check`: 수정 JS 7개 파일 문법 검사 통과.
- `node .claude/scripts/validate.mjs 1000`: 총 위반 사례 0건.
- `node .claude/scripts/audit.mjs`: 깨진 refs 0건, ext 승격 후보 0건, 라벨 중복 0건. 기존 선수학습 미등록 정보 5건은 유지.
- 브라우저 자동 점검: 5개 곱셈구구 단 선택 학습지 × 2~9단 총 40개 조합에서 선택 단 불일치 0건, overflow 0건.
- 5-1 2단원 `곱셈구구` 2단/9단 화면 캡처 확인.

## 2026-05-06: 가로셈 dense 학습지 자동 문제 수 보정

### 변경 내용

- 가로셈 문제의 자동 맞춤 판정이 답칸 높이를 줄바꿈으로 오판해 36문항짜리 dense 학습지를 1문항으로 줄이던 문제를 수정.
- `곱셈구구`, `나눗셈 (한 자리 나누기)`, 1학년 받아올림/받아내림 가로셈 등 같은 유형 16개 학습지가 정상 문제 수로 잡히도록 보정.
- 기존 탭/브라우저 캐시가 예전 JS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신.

### 검증

- 전체 244개 일반 학습지 브라우저 자동 맞춤 전수 스캔: `safe <= 2` 의심 항목 0건, hard 대비 25% 이하 저효율 항목 0건.
- 기존 문제 16개 dense 학습지 모두 `36`문항으로 회복 확인.
- 대표 5개 학습지(`5-1 곱셈구구`, `5-1 나눗셈`, `2-2 곱셈구구 혼합`, `1-2 받아올림 가로셈`, `1-2 받아내림 가로셈`) 화면/정답/인쇄 overflow 0건 확인.
- `node .claude/scripts/validate.mjs 1000`: 총 위반 사례 0건.
- `node .claude/scripts/audit.mjs`: 깨진 refs 0건, ext 승격 후보 0건, 라벨 중복 0건. 기존 선수학습 미등록 정보 5건은 유지.

## 2026-05-06: 5학년 1학기 선수학습 진단지 추가

### 변경 내용

- 5학년 1학기 1·2·4·5·6단원 학습지 목록 맨 앞에 `[진단지] 선수학습 진단지`를 추가.
- 각 선수학습 항목에서 4문항씩 뽑아 A4 2단 compact 진단지로 출력하도록 추가.
- 한 단 24문항, 한 장 48문항 기준으로 배치하고, 1단원은 52문항이라 2장으로 자동 분할.
- 선수학습이 없는 3단원에는 진단지를 추가하지 않음.
- 기존 탭/브라우저 캐시가 예전 JS/CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신.

### 검증

- `node .claude/scripts/validate.mjs 1000`: 총 위반 사례 0건.
- `node .claude/scripts/audit.mjs`: 깨진 refs 0건, ext 승격 후보 0건, 라벨 중복 0건. 기존 선수학습 미등록 정보 5건은 유지.
- Playwright headless 브라우저에서 5-1 1·2·4·5·6단원 진단지 첫 항목 배치, 문항 수, 정답 보기, overflow 0건 확인.
- Playwright print media에서 5-1 1단원 진단지 52문항/2장, overflow 0건 확인.

## 2026-05-05: 분수 계산 과정 한 줄화와 빈칸 크기 통일

### 변경 내용

- 분수 계산 과정에서 `line1/line2/line3`으로 나뉘던 단계 풀이를 한 개의 가로 흐름으로 이어서 표시하도록 변경.
- 분수 계산 과정 학습지를 1열 배치 대상으로 확장해, 한 줄 풀이가 카드 안에서 잘리지 않도록 조정.
- 분수 빈칸의 분자칸·분모칸 크기를 기존 쓰기칸 기준인 `1.85em`으로 통일.
- 분수 빈칸의 가로선도 같은 폭을 쓰도록 조정해, 분수의 곱셈·나눗셈 계산 과정에서 칸 폭이 숫자 길이에 따라 흔들리지 않게 함.
- 기존 탭/브라우저 캐시가 예전 JS/CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신.

### 검증

- JS 문법 검사 통과.
- Chrome headless에서 4-2 분수 단계 풀이 6종, 5-1 분수 단계 풀이 11종, 5-2 분수 곱셈 계산 과정 7종, 6-1 분수 나눗셈 계산 과정 3종, 6-2 분수 나눗셈 계산 과정 3종 확인.
- 30종 전체 확인 결과: 3줄 DOM 0개, 각 문제당 풀이 흐름 1개, 줄바꿈/넘침 0건, 분자칸·분모칸·자연수칸·분수 가로선 폭 `22.38px`로 일치.

## 2026-05-05: 소수 격자 디자인 보정

### 변경 내용

- 소수점이 찍히는 위치의 별도 진한 세로선을 제거하고, 소수점 점만 기존 격자선 위에 표시되도록 조정.
- 소수 곱셈·나눗셈 격자 내부의 추가 진한 가로선을 제거하고 기본 격자만 남김.
- 6학년 2학기 소수 나눗셈에서 세로셈 위에 표시되던 불필요한 가로셈식 출력을 제거.
- 6학년 1학기·2학기 소수 나눗셈 학습지는 선택 시 글자 크기 기본값이 `크게`로 설정되도록 조정.
- 기존 탭/브라우저 캐시가 예전 JS/CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신.

### 검증

- JS 문법 검사 통과.
- 브라우저에서 `worksheet.html?v=20260505-decimal-clean`로 5-2 소수 곱셈, 6-1 소수 나눗셈, 6-2 소수 나눗셈 스크린샷 확인.
- 6-2 소수 나눗셈 3개 학습지 전체 확인: 글자 크기 `크게`, `.decimal-shift-row` 0개, 내부 진한 작업선 0개, 가로셈식 `=` 표시 0건.

## 2026-05-05: 소수 격자 CSS Grid 재구현

### 변경 내용

- 소수의 곱셈·나눗셈 격자를 `table` 기반 렌더링에서 CSS Grid 기반 렌더링으로 교체.
- 숫자는 칸 중앙에 두고, 소수점은 별도 점 요소로 숫자와 숫자 사이의 세로선 위에 배치.
- 나눗셈에서 나눠지는 수는 소수점 여부와 관계없이 작업 칸의 첫 칸부터 왼쪽 정렬되도록 수정.
- 기존 탭/브라우저 캐시가 예전 JS/CSS를 계속 쓰지 않도록 `worksheet.html`의 파일 버전 쿼리를 갱신.

### 검증

- JS 문법 검사 통과.
- 브라우저에서 `worksheet.html?v=20260505-gridline`로 5-2 소수 곱셈 대표 학습지 스크린샷 확인.
- 브라우저에서 6-1 자연수 ÷ 자연수 대표 학습지 스크린샷 확인: 나눠지는 수가 작업 칸 첫 칸부터 배치됨.

## 2026-05-04: 소수 세로셈 격자와 분수 나눗셈 계산 과정 추가

### 변경 내용

- 5학년 2학기 `소수의 곱셈` 학습지를 한 줄 계산식에서 세로셈 격자형 출력으로 변경.
- 6학년 1학기·2학기 `소수의 나눗셈` 학습지를 세로 나눗셈 격자형 출력으로 변경.
- 6학년 2학기 소수 나눗셈은 나누는 수를 자연수로 바꾼 식을 격자 위에 함께 표시하도록 조정.
- 6학년 1학기·2학기 `분수의 나눗셈`에 기존 계산지와 별도로 `계산 과정` 학습지를 추가.
- 분수 나눗셈 과정은 원식, 분수 변환, 역수 곱셈, 결과 정리 순서로 연습하도록 `T.fracDivStep` 템플릿 추가.
- 소수 세로셈/세로 나눗셈 격자용 렌더러와 인쇄용 CSS 추가.
- 과정형 분수 나눗셈 학습지는 1열 4문제 배치로 고정해 인쇄 잘림 위험을 낮춤.
- 자동 맞춤 측정에서 소수 격자 테이블의 테두리 두께가 overflow로 오인되지 않도록 보정.

### 검증

- JS 문법 검사 통과.
- 관련 제너레이터/렌더러 80,000회 생성 검사: `undefined`, `NaN`, 음수 정답 0건.
- 카탈로그 로드 확인: 244개 학습지, 새 계산 과정/소수 격자 학습지 ID 확인.
- 브라우저에서 5-2 소수 곱셈, 6-1 소수 나눗셈, 6-2 소수 나눗셈, 6-1/6-2 분수 나눗셈 계산 과정 대표 학습지 생성 및 정답 표시 확인.
- 대표 학습지 브라우저 overflow 확인: A4 시트 넘침 0건.

## 2026-05-04: 1학년 수 세기 학습지 십진 모형 그림화

### 변경 내용

- `50까지의 수 세기`, `100까지의 수 세기` 문제를 문장 설명 대신 십 모형·낱개 그림을 보고 아래 답칸에 수를 쓰는 형태로 변경.
- `9까지의 수 세기`에서 각 문제마다 반복되던 “모두 몇 개인가요?” 안내 문장을 제거하고 점 그림과 답칸만 남김.
- 공용 십진 모형 HTML 헬퍼(`baseTenModelHtml`, `placeValueModelHtml`)와 전용 표시 스타일(`.base-ten-model`, `.model-count-card`) 추가.
- 2학년 1학기 1단원에 `모형 보고 세 자리 수 쓰기` 학습지 추가.
- 2학년 2학기 1단원에 `모형 보고 네 자리 수 쓰기` 학습지 추가.
- 그림 보고 숫자 쓰기 유형의 답칸을 일반 답칸보다 크게 조정: 높이 `2.15em`, 최소 너비 `3.6em`.
- 십 모형은 낱개보다 크게 보이도록, 백 모형은 10×10 평면 격자로, 천 모형은 옅은 두께가 있는 입체 블록으로 표시.
- `design.md`에 “-까지의 수” 그림 문제 원칙과 검수 기준 추가.

### 검증

- JS 문법 검사 통과.
- `audit.mjs`: 깨진 prereq 0건, ext 승격 후보 0건, 라벨 중복 0건.
- `validate.mjs 5000`: 위반 0건.
- 생성 HTML 불변식 확인: `base-ten-model` 포함, 기존 “인 수” 문장 미포함, 아래 답 영역 포함.
- 생성 HTML 불변식 확인: 2학년 `모형 보고 세 자리 수 쓰기`, `모형 보고 네 자리 수 쓰기`에 `model-count-card`, `base-ten-model`, 답칸 포함 및 불필요한 문장 미포함.
- 브라우저에서 `50까지의 수 세기`, `100까지의 수 세기`, `모형 보고 세 자리 수 쓰기`, `모형 보고 네 자리 수 쓰기`가 모형 그림과 아래 답칸으로 렌더링되는지 확인.

## 2026-05-04: 1·2학년 가르기 풀이 디자인 원칙 정리 및 칸 크기 수정

### 변경 내용

- `design.md` 추가:
  - 가르기 칸과 최종 정답 칸은 같은 크기여야 함.
  - 문제 구획용 회색 네모 박스·카드 테두리 금지.
  - 인쇄 효율을 우선하는 검수 기준 명시.
- 1·2학년 받아올림·받아내림 가르기 다이어그램의 작은 칸을 최종 정답 칸과 같은 쓰기칸 기준으로 통일.
- 전체 숫자 쓰기칸 기준을 고정 `48px`에서 실제 글자 크기 대비 `1.85em` 비율로 전환해, 폰트 대비 과하게 큰 네모 박스를 줄임.
- 가르기 칸, 단계 풀이 정답 칸, 원본 가로 연습 정답 칸의 테두리 두께와 둥근 정도를 통일.
- 1·2학년 7단원 가르기형 가로 연습지는 동일한 쓰기칸 크기 때문에 3열에서 문제끼리 겹치지 않도록 2열 배치로 고정.
- 원본 세로 연습의 가르기 입력칸(`.origin-input-circle`)도 같은 쓰기칸 크기와 현재 폰트 수준으로 통일하고, 가지선 좌표를 큰 칸에 맞게 조정.
- 단계 풀이·개념·도형 컨테이너의 회색 카드 테두리와 배경을 제거해 문제 구획 박스가 인쇄되지 않도록 수정.
- 문제 수 입력칸을 직접 숫자를 타이핑할 수 있는 숫자 전용 텍스트 입력으로 변경하고, 입력 중 숫자가 아닌 문자는 제거하도록 수정.
- 문제 수 입력칸에서 Enter를 누르면 `새로 만들기`와 동일하게 학습지를 다시 생성하도록 수정.

### 검증

- JS 문법 검사 통과.
- 원본 `index (1).html` 옵션명 대조: 누락 0건.
- `audit.mjs`: 깨진 prereq 0건, ext 승격 후보 0건, 라벨 중복 0건.
- `validate.mjs 5000`: 위반 0건.
- 실제 브라우저에서 `[본단원] 한 자리 수 + 한 자리 수(받아올림) - 가로 연습`, `[본단원] 십몇 - 몇(받아내림) - 가로 연습`, `[본단원] (십몇) − (몇), 받아내림`, `[본단원] 십몇 - 몇(받아내림) - 연습` 화면과 정답 표시 확인.
- `design-css-html-check`: `design.md`의 `.small-box`, `.origin-answer-box`, `.origin-input-circle`, `.concept-card` 규칙과 1-2-7단원 생성 HTML 구조 확인.
- 로컬 테스트 URL `http://127.0.0.1:8010/worksheet.html` 응답 200 확인.

## 2026-05-03: `index (1).html` 1·2학년 원본 연습형 이식

### 변경 내용

- `/Users/jiheepark/Downloads/index (1).html` 의 1·2학년 연습지 옵션을 현재 카탈로그에 개별 학습지로 이식.
- 추가된 원본 연습형:
  - 한 자리 덧셈 받아올림: 가로 연습, 세로 연습, 가로, 세로
  - 십몇 빼기 받아내림: 가로 연습, 세로 연습, 가로, 세로
  - 두 자리 + 한 자리 받아올림: 가로 연습, 세로 연습, 세로
  - 두 자리 - 한 자리 받아내림: 세로 연습, 연습 2, 세로
  - 두 자리 ± 두 자리: 받아올림/받아내림 연습, 연습 2, 세로, 혼합
- 원본의 체리 다이어그램, 가르기 가로 연습, 받아내림 `10` 박스 레이아웃을 현재 정답 토글(`data-ans`) 방식으로 연결.
- 원본 라벨 그대로 `10의 보수 (가로셈 빈칸)`, `곱셈구구 (기본)`, `곱셈구구 (빈칸)` 항목 추가.
- 원본 구구단 모달은 기존 결정에 따라 가져오지 않고, 이미 구현된 단별 학습지/혼합/빈칸형 구조를 유지.

### 검증

- 원본 옵션명 대조: 누락 0건.
- 카탈로그 로드: 12개 학기, 231개 학습지.
- `audit.mjs`: 깨진 prereq 0건, ext 승격 후보 0건, 라벨 중복 0건.
- `validate.mjs 5000`: 위반 0건.

## 2026-05-03: 1~6학년 수와 연산 범위 확장

### 변경 내용

- 수와 연산 영역 기준으로 누락 학기·단원을 보강:
  - 1-1: 9까지의 수, 50까지의 수
  - 1-2: 100까지의 수
  - 2-1: 세 자리 수, 곱셈(1)
  - 2-2: 네 자리 수
  - 3-1: 분수와 소수
  - 4-1: 큰 수
  - 4-2: 소수의 덧셈과 뺄셈
  - 5-2: 소수의 곱셈
  - 6-1: 분수의 나눗셈(3), 소수의 나눗셈(4)
  - 6-2: 분수의 나눗셈(5), 소수의 나눗셈(6)
- 문제 칸 내부에서 문제 일부가 세로 가운데에서 시작하지 않도록 `spacious` 레이아웃을 위쪽 정렬로 수정.
- 6학년 1·2학기 카탈로그와 generator 폴더 추가.
- 소수 단원 정답을 회귀 테스트가 오탐하지 않도록 `validate.mjs` 를 단원명 기준으로 보정.
- 5-1 약분과 통분의 `[3-1] 분수와 소수` 선수학습 참조를 구현된 `unitRef('g3-1', 'u6')` 로 승격.

### 검증

- JS 문법 검사 통과.
- 카탈로그 로드: 12개 학기, 204개 학습지.
- `audit.mjs`: 깨진 prereq 0건, ext 승격 후보 0건, 라벨 중복 0건.
- `validate.mjs 5000`: 위반 0건.

## 2026-05-01: 학년·학기 다중 지원 구조로 재편

### 배경
- 1학년 1학기부터 6학년 2학기까지 12개 학기 확장 계획
- 기존 단원 ID(`u1` 등)는 학기 사이에서 충돌
- 단일 `catalog.js`에 모든 학기 정의가 들어가면 파일이 비대해짐

### 변경 내용

#### 1. 폴더 구조 재편

| 기존 | 변경 후 |
|---|---|
| `js/generators/u1.js` … `u6.js` | `js/generators/g5-1/u1.js` … `u6.js` |
| (없음) | `js/catalogs/g5-1.js` (학기별 카탈로그) |
| `js/catalog.js` (모든 정의 포함) | `js/catalog.js` (빌더 + 학기 통합 진입점) |

#### 2. 데이터 구조 변경

- 카탈로그 항목에 `grade` 필드 추가: `{ grade: 'g5-1', unit: 'u1', ... }`
- 학습지 ID에 학기 prefix 자동 부착: `u1_pre_add_2d` → `g5-1_u1_pre_add_2d`
- `UNIT_META` (1단계 객체) → `GRADE_META` (학기→단원 2단계 구조)
- `defineUnit(unitId, ...)` → `defineUnit(gradeId, unitId, ...)`
- `getWorksheetsByUnit(unitId)` → `getWorksheetsByUnit(gradeId, unitId)`
- 새 헬퍼: `getGradeMeta`, `getUnitMeta`, `getUnitsOfGrade`

#### 3. UI 변경

- 컨트롤바에 **학년·학기** 드롭다운 추가 (현재 옵션은 5-1만)
- 학기 변경 시 단원 선택지 자동 재구성

#### 4. 신규 학기 추가 절차

1. `js/generators/g{학년}-{학기}/uN.js` 작성
2. `js/catalogs/g{학년}-{학기}.js` 작성 (`g5-1.js` 형식 참고)
3. `js/catalog.js` 의 `GRADES` 배열에 `import * as ... ` 추가

### 검증
- 79개 학습지 그대로 유지, 모든 ID 정상 prefix 부착
- PDF 매핑 무결 (12개 매핑 모두 카탈로그와 일치)
- node syntax check, 동적 import 로드 모두 정상

### 영향받은 파일
- 이동: `app/js/generators/u{1..6}.js` → `app/js/generators/g5-1/u{1..6}.js`
- 신규: `app/js/catalogs/g5-1.js`
- 수정: `app/js/catalog.js`, `app/js/app.js`, `app/js/renderers.js` (UNIT_META import 제거), `app/worksheet.html`
- 함께 추가: 프로젝트 루트 `CURRICULUM.md` (1~6학년 계열표)

---

## 2026-05-01: 모듈화 리팩토링 + 템플릿 시스템 도입

### 배경
- 기존 `worksheet.html` 단일 파일 3,052줄에 HTML/CSS/JS가 전부 포함
- 학습지 추가 시 제너레이터 + 렌더링 + 카탈로그를 매번 수동 작성해야 하는 비효율
- 학년 확장(1~6학년) 시 구조적으로 불가능한 상태

### 변경 내용

#### 1. 파일 분리 (1개 -> 13개 모듈)

| 기존 | 변경 후 | 줄 수 | 역할 |
|------|--------|------|------|
| worksheet.html (3052줄) | worksheet.html | 74 | HTML 껍데기만 |
| (같은 파일 내) | css/worksheet.css | 746 | 스타일 전체 |
| (같은 파일 내) | js/utils.js | 105 | 수학 유틸 (rand, gcd, lcm, simplify 등) |
| (같은 파일 내) | js/helpers.js | 153 | HTML 조각 생성 (fracD, numBlank, SVG 도형 등) |
| (같은 파일 내) | js/templates.js | 397 | 풀이 과정 템플릿 시스템 |
| (같은 파일 내) | js/layout.js | 133 | 그리드 레이아웃 엔진 |
| (같은 파일 내) | js/renderers.js | 277 | 문제 렌더링 + 시트 조립 |
| (같은 파일 내) | js/catalog.js | 413 | 카탈로그 빌더 + 학습지 목록 |
| (같은 파일 내) | js/app.js | 102 | UI 이벤트 + 초기화 |
| (같은 파일 내) | js/generators/u1.js | 186 | 1단원 제너레이터 |
| (같은 파일 내) | js/generators/u2.js | 53 | 2단원 제너레이터 |
| (같은 파일 내) | js/generators/u3.js | 38 | 3단원 제너레이터 |
| (같은 파일 내) | js/generators/u4.js | 94 | 4단원 제너레이터 |
| (같은 파일 내) | js/generators/u5.js | 112 | 5단원 제너레이터 |
| (같은 파일 내) | js/generators/u6.js | 68 | 6단원 제너레이터 |

#### 2. 템플릿 시스템 도입 (templates.js)

**핵심 변화**: 데이터(수학 로직)와 표현(풀이 과정 HTML)을 분리

기존:
```js
// 제너레이터가 수학 + 렌더링 전부 담당
function genU5MainAdd() {
  let d1, d2, n1, n2;
  // ... 숫자 생성
  // ... 통분 과정 HTML 직접 조립
  // ... 결과 포맷팅
  return fracStepProblem(steps);  // HTML이 섞인 결과
}
```

변경 후:
```js
// 제너레이터: 순수 데이터만
function genFracAddLt1() {
  return { n1, d1, n2, d2, op: '+' };
}

// 템플릿: 풀이 과정 렌더링만 (재사용 가능)
T.fracLcdStep.render(data)  // 통분 단계별 풀이
```

정의된 템플릿 목록:
- `T.horizontal` - 가로셈 (expression = answer)
- `T.vertical` - 세로셈 (사칙연산)
- `T.longDiv` - 나눗셈 세로셈
- `T.concept` - 개념 카드 (질문 + 답)
- `T.relation` - 관계 테이블
- `T.shape` - 도형 넓이/둘레
- `T.fracCompare` - 분수 크기 비교
- `T.fracConvert` - 분수 변환
- `T.fracLcdStep` - 진분수 통분 연산 (1줄 풀이)
- `T.mixedImproperStep` - 대분수 연산, 가분수 변환 (1줄)
- `T.mixedSeparateStep` - 대분수 연산, 따로 계산 (3줄)
- `T.divMethod` - 나눗셈법 (최대공약수/최소공배수)
- `T.pdfGrid` - PDF 격자 세로셈
- `T.raw` - 자유 HTML (호환용)

#### 3. 카탈로그 빌더 (catalog.js)

기존:
```js
// 학습지마다 9개 필드 수동 입력
{ id: 'u1_pre_add_2d', unit: 'u1', section: '선수학습',
  label: '두 자리 수 덧셈', lessonRef: '선수학습',
  kind: 'pdf', grid: 'standard', count: 20,
  generator: genU1PreAdd2d },
```

변경 후:
```js
// 공통 속성은 그룹에서 상속
defineUnit('u1', '자연수의 혼합 계산', [
  선수학습({ kind: 'pdf', grid: 'standard', count: 20 }, [
    학습지('두 자리 수 덧셈', genU1PreAdd2d),
    학습지('두 자리 수 뺄셈', genU1PreSub2d),
  ]),
  차시('1차시 덧셈과 뺄셈', [
    학습지('혼합(순서)', genU1MainAddSubOrder),
  ]),
]);
```

빌더 함수:
- `defineUnit(unitId, unitName, groups)` - 단원 정의
- `선수학습(defaults, items)` - 선수학습 그룹
- `차시(lessonRef, [defaults], items)` - 차시(본단원) 그룹
- `학습지(label, [template], generator, [overrides])` - 개별 학습지

#### 4. 기술 스택

- ES Modules (`import`/`export`) 사용 - 빌드 도구 불필요
- `<script type="module">` 로 브라우저 네이티브 지원
- Netlify 배포 구조 유지 (정적 파일만)

### 향후 확장 방향

1. **학년 추가**: `generators/` 에 파일 추가 + `catalog.js`에 등록
2. **새 템플릿**: `templates.js`에 `T.newTemplate` 추가
3. **AI 연동**: 풀이 과정 이미지 -> 템플릿 자동 매칭 -> 제너레이터 생성
