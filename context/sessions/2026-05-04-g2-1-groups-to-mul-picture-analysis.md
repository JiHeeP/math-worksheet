# 분석 기록: 2-1 곱셈 묶음 그림 문제로 변경

- **일시**: 2026-05-04
- **요구사항 원문**: 2-1 곱셈의 "묶음을 곱셈식으로 나타내기" 학습지에서 `n개씩 m묶음` 텍스트 대신 그림이 나오게 하고, `n × m = □` 대신 아무 문구 없는 밑줄만 두기.
- **판정**: ✅ 안전

## 영향 범위

| 대상 | 영향 수준 | 상세 |
|---|---|---|
| `app/js/generators/g2-1/u6.js` | 낮음 | 해당 학습지 생성 함수 하나를 바꾸면 됨. |
| `app/css/worksheet.css` | 낮음 | 묶음 그림과 긴 밑줄 모양을 위한 전용 스타일이 필요할 수 있음. |
| 다른 학습지 | 없음 | 카탈로그 id와 단원 구조는 그대로 둠. |

## 점검 결과

| 점검 항목 | 결과 | 근거 |
|---|---|---|
| 기존 약속 | ✅ | `genG21U6GroupsToMul()` 함수명과 catalog id `u6_main_groups_to_mul`은 유지 가능. |
| 연결 관계 | ✅ | generator가 HTML 문제를 반환하는 기존 방식 안에서 처리 가능. |
| 데이터 모양 | ✅ | 저장 데이터나 JSON 구조를 바꾸지 않음. |
| 과거 기록 | ✅ | 기존 생성 결과를 저장하는 구조가 없음. |
| 사용자 흐름 | ✅ | 학년/단원/학습지 선택 흐름은 그대로 유지됨. |
| 되돌리기 어려움 | ✅ | 코드와 CSS만 수정하므로 쉽게 되돌릴 수 있음. |

## 위반 상세

- 없음.

## 기술 요구사항

| 순서 | 작업 유형 | 대상 | 설명 | 의존 순서 |
|---|---|---|---|---|
| 1 | BUG_FIX | `app/js/generators/g2-1/u6.js` | `genG21U6GroupsToMul()`이 텍스트 대신 묶음 그림 HTML을 만들도록 변경. | — |
| 2 | THEME_CHANGE | `app/css/worksheet.css` | 묶음 그림과 정답용 밑줄 전용 스타일 추가. | 1 |
| 3 | BUG_FIX | 정답 토글 | 밑줄에 정답 식을 `data-ans`로 보관해서 정답 보기 때 식이 보이게 함. | 1 |

## 제시한 대안

- 전용 템플릿을 새로 만들 수도 있지만, 영향 범위를 줄이기 위해 우선 `u6.js`에서 이 학습지 전용 HTML을 만드는 방식이 가장 안전함.
