# 디자인 원칙

## 전역 쓰기칸 비율

- 학습지 전체에서 숫자 네모 박스는 고정 px가 아니라 해당 칸의 실제 글자 크기를 기준으로 잡는다.
- 기본 숫자 쓰기칸 비율은 `font-size × 1.85`이다. 코드에서는 `--write-box-ratio: 1.85`와 `--write-box-size: 1.85em` 기준을 사용한다.
- 박스는 원 학습지 글자보다 커야 하지만, `font-size × 2`를 크게 넘으면 박스가 과하게 커 보이므로 기본 답칸에서는 피한다.
- 비교기호 칸은 숫자 쓰기칸보다 작게 `font-size × 1.7` 수준을 사용한다.
- 분수의 분자·분모 쓰기칸은 세로 공간을 아껴야 하므로 기존에 적정했던 `font-size × 1.6` 수준을 유지한다.
- 긴 답이나 단위가 붙는 답은 높이는 기본 비율을 따르고, 너비만 내용 길이에 맞춰 넓힌다.

## 1·2학년 받아올림·받아내림 연습지

- 가르기 다이어그램의 두 칸과 최종 정답 칸은 같은 크기여야 한다.
- 코드 기준으로 가로셈 가르기 칸은 `.small-box`, 가로셈 최종 답 네모칸은 `.origin-answer-box`이다. 두 클래스는 같은 너비, 같은 높이, 같은 글자 크기, 같은 테두리 두께를 사용해야 한다.
- 현재 CSS의 글자 크기 체계에 맞춰 두 칸 모두 해당 요소의 실제 `font-size`와 전역 쓰기칸 비율을 기준으로 삼는다. 칸 크기를 `48px` 같은 고정 픽셀값으로 되돌리지 않는다.
- 예: `9 + 9 = □`에서 뒤의 `9`를 두 수로 가를 때, 가르기 칸 2개와 최종 답 칸 1개는 모두 동일한 정사각형 크기여야 한다.
- 가르기 칸은 학생이 실제로 숫자를 쓰는 입력 자리이므로, 작은 보조 표시처럼 보이면 안 된다.
- 가르기 가지선은 칸 크기에 맞춰 넓게 벌리고, 두 칸 사이 간격은 답 쓰기 공간을 침범하지 않을 정도로 둔다.
- 가로 가르기 연습과 가르기 풀이에서는 3열 배치를 강제하지 않는다. `.small-box`와 `.origin-answer-box`를 같은 크기로 유지한 상태에서 문제끼리 겹치면, 칸을 줄이지 말고 2열처럼 더 넓은 배치를 사용한다.
- 세로셈의 최종 답 줄 `.origin-answer-row`는 네모칸으로 만들지 않는다. 이 규칙은 1·2학년 7단원 가로 연습의 `.small-box`와 `.origin-answer-box` 크기 일치에 관한 것이다.
- 세로셈의 가르기 입력칸은 `.origin-input-circle`이다. 이 칸도 작은 보조 표시가 아니라 학생이 숫자를 쓰는 자리이므로, 현재 폰트 수준과 쓰기칸 크기 기준에 맞춰야 한다.
- `.origin-input-circle`은 `.small-box` 및 `.origin-answer-box`와 같은 글자 크기 체계와 전역 쓰기칸 비율을 따른다.

## 인쇄 효율

- 문제 하나하나를 회색 네모 박스나 카드 테두리로 구획하지 않는다.
- 풀이 과정 문제를 감싸는 `.concept-card`는 배경과 테두리를 모두 투명 처리한다. CSS 기준은 `background: transparent`, `border: 0 solid transparent`, `box-shadow: none`이다.
- 인쇄물에서는 식, 문제 번호, 쓰기 칸만으로도 문제 구획이 충분히 드러나야 한다.
- 테두리는 학생이 답을 쓰는 칸, 비교 기호 칸, 계산에 필요한 표·격자처럼 학습 행위에 필요한 요소에만 쓴다.
- 풀이 과정용 컨테이너는 배치만 담당하고, 배경색·회색 테두리·장식용 카드 스타일을 넣지 않는다.

## 줄바꿈 없는 식 배치

- 가로 수식, 분수식, 계산 과정은 가능하면 한 줄에 온전히 들어가야 한다. 식 중간에서 줄바꿈되어 `=`, 연산 기호, 분수 일부가 다음 줄로 밀리면 수정 대상이다.
- 줄바꿈이 생길 때는 답칸·분수칸·글자 크기를 먼저 줄이지 않는다. 문제 수를 줄이거나, 3~4열 배치를 2열/1열처럼 넓은 배치로 바꾸는 것을 우선한다.
- 과정 풀이 학습지는 기본적으로 넓은 칸을 써야 한다. 코드 기준으로 `T.fracLcdStep`, `T.mixedImproperStep`, `T.mixedSeparateStep`, `T.fracMulStep` 같은 풀이 과정 템플릿은 `wide` 배치와 8문항 기준을 기본으로 삼는다.
- 같은 차시 안의 기본 연습지가 `practice` 15문항이어도, 계산 과정 학습지가 그 설정을 그대로 상속하면 안 된다. 과정형 학습지는 별도로 `grid: 'wide'`, `count: 8`처럼 안전한 배치를 명시한다.
- 한 문제 안에서 줄바꿈을 허용해야 할 만큼 식이 길다면, 먼저 식을 두 줄 문제로 설계할 이유가 있는지 확인한다. 단순히 칸이 좁아서 생긴 줄바꿈은 디자인 실패로 본다.
- `.horiz-box`, `.frac-row`, `.frac-step-flow` 같은 식 컨테이너는 렌더링 검수 때 실제 줄 수를 확인한다. 정답 보기 전과 후 모두 줄바꿈, 겹침, 밀림이 없어야 한다.
- 인쇄 효율보다 가독성이 우선이다. 한 장에 문제를 더 많이 넣으려고 식을 꺾거나 답칸을 작게 만들지 않는다.

## 그림 보고 숫자 쓰기 문제

- `50까지의 수 세기`, `100까지의 수 세기`, `세 자리 수`, `네 자리 수`처럼 모형을 보고 수를 쓰는 문제는 문장 설명을 기본으로 두지 않는다.
- `9까지의 수 세기`도 점 그림과 아래 답칸만 둔다. 각 문제 안에 “모두 몇 개인가요?” 같은 안내 문장을 반복하지 않는다.
- “십 모형 2개와 낱개 2개인 수”라고 쓰는 대신, 실제 십 모형 2개와 낱개 2개를 그림으로 배치하고 그 아래에 숫자 답칸을 둔다.
- 코드 기준으로 십진 모형 그림은 `.base-ten-model`, 천 모형은 `.base-ten-thousands`, 백 모형은 `.base-ten-hundreds`, 십 모형은 `.base-ten-rods`, 낱개 묶음은 `.base-ten-ones`이다.
- 십 모형은 하나의 긴 막대 안에 10칸이 보이도록 그려야 하며, 낱개는 독립된 작은 정사각형으로 보여야 한다. 양감이 드러나도록 십 모형은 낱개보다 길고 크게, 낱개는 쓰기칸과 경쟁하지 않을 정도로 작게 둔다.
- 세 자리·네 자리 수에서는 백 모형은 10×10 격자 평면, 천 모형은 살짝 두께가 보이는 입체 블록으로 표현한다. 단, 장식용 그림자가 아니라 자릿값 모형의 부피 차이를 보여주는 선과 아주 옅은 면만 사용한다.
- 세 자리·네 자리 수의 자릿값 모형은 빠르게 세기 쉽도록 5개 단위로 끊어 배열한다. 네 자리 수처럼 모형이 많아지는 경우 천·백 같은 큰 단위와 십·일 같은 작은 단위를 억지로 한 줄에 붙이지 않고 위아래 두 줄로 나누어 높이와 시선 이동을 줄인다.
- 답칸은 그림의 오른쪽이나 같은 줄이 아니라 아래쪽에 위치해야 한다. 이 유형의 문제 컨테이너는 `.model-count-card`를 사용한다.
- 그림 보고 숫자 쓰기 전용 답칸은 일반 숫자 답칸보다 커야 한다. 기준은 높이 `font-size × 2.15`, 최소 너비 `font-size × 3.6`이며 코드에서는 `--model-answer-box-height: 2.15em`, `--model-answer-box-min-width: 3.6em`을 사용한다.
- 그림 자체의 선은 학습 대상인 십 모형·낱개를 표현하기 위한 선이므로 허용하지만, 문제 구획용 회색 카드 배경이나 테두리는 넣지 않는다.

## 검수 체크

- 1·2학년 7단원 가로 연습에서는 `.small-box`와 `.origin-answer-box`의 실제 렌더링 크기가 동일해야 한다.
- 전체 학습지에서 기본 숫자 네모 박스의 높이·너비는 해당 칸 폰트 크기의 약 `1.85배`여야 한다.
- 1·2학년 7단원 세로 연습에서는 `.origin-input-circle`이 현재 폰트 수준과 전역 쓰기칸 비율에 맞아야 한다.
- 정답 보기 전에도 가르기 칸과 정답 칸 크기가 같은지 확인한다.
- 정답 보기 후에도 칸 크기와 줄 배치가 흔들리지 않아야 한다.
- 한 문제의 풀이 일부가 칸 가운데에서 시작하지 않고 위쪽부터 배치되어야 한다.
- 가로 수식, 분수식, 계산 과정에서 `=`, 연산 기호, 분수 일부가 다음 줄로 넘어가면 수정 대상이다.
- 줄바꿈을 없애기 위해 쓰기칸을 과하게 줄이면 안 된다. 배치 열 수나 기본 문제 수를 먼저 조정한다.
- `.concept-card`에 회색 배경, 회색 테두리, 그림자 효과가 보이면 수정 대상이다.
- 인쇄 미리보기에서 문제 구획용 회색 박스가 보이면 수정 대상이다.
- `50까지의 수 세기`, `100까지의 수 세기`는 학생이 문장을 읽고 푸는 형태가 아니라 십 모형·낱개 그림을 보고 아래 답칸에 수를 쓰는 형태여야 한다.
- `2-1` 1단원에는 `모형 보고 세 자리 수 쓰기`, `2-2` 1단원에는 `모형 보고 네 자리 수 쓰기` 학습지가 있어야 한다.
- 수 세기 그림 문제의 개별 문제 안에 “모두 몇 개인가요?”처럼 학습지 제목과 중복되는 안내 문장이 보이면 수정 대상이다.
